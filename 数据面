一、计算机体系结构（性能根基）
核心：理解 CPU 如何执行代码、如何访问内存、如何处理缓存一致性
重点知识：
1 CPU
  1.1.1 指令流水线
    CPU 指令流水线（Instruction Pipeline）是现代处理器提升性能的核心机制之一。其核心思想是：将一条指令的执行过程拆分成多个阶段，使多条指令在不同阶段并行执行，从而提高吞吐量。
    CPU硬件组成分为各种单元，可以按照顺序将指令分割，在相应的硬件单元处理。

    类比：工业流水线，生产一辆车不需要等待上一辆完成才能开始下一辆，而是不同工人同时在不同工序上作业。

    1）假设一条指令执行需要 5 个步骤：
      (1)取指令 IF(Instruction Fetch)
        CPU 必须知道下一条指令在哪里 → 用 PC 指向内存地址
        硬件：PC、I-Cache、ITLB
        例子：PC = 0x1000 → 从 I-Cache 读取 ADD R1, R2, R3

      (2)译码（Instruction Decode, ID），
          CPU 要知道指令要做什么 → Opcode、源寄存器、目的寄存
          硬件：Decoder + Register File + Control Unit
          例子：ADD R1, R2, R3 → 读 R2、R3，生成 ALU 控制信号
      补充：具体做哪事？
        1）解析操作码：识别这是加法、减法、还是加载指令？
        2）生成控制信号：这是最关键的输出。译码器根据指令类型，生成一串控制信号（如RegWrite、MemRead、MemWrite、ALUSrc、Branch等）。如果控制信号==0，就不做相应操作。
        3）读取寄存器：同时，译码器会找出指令中涉及的寄存器编号（比如R1、R2），并立即从寄存器堆中读出这些寄存器的当前值。
        4）扩展立即数：如果指令中包含立即数（比如addi R1， R2， 100中的100），译码阶段会把这个数从指令中提取出来，并扩展成32位或64位的标准长度

      (3)执行（Execute, EX）
        CPU 做真正计算
        目的：算术逻辑运算、地址计算、分支判断
        硬件：ALU、FPU、AGU、Branch Unit
        例子：R1 = R2 + R3 或 计算 LOAD 地址 R2 + offset
      补充： cpu运算单元包括，ALU(算术逻辑单元,) / AGU(地址生成单元 Address Generation Unit) /FPU(浮点运算单元,Floating Point Unit)
            AGU:专门负责计算内存地址
            FPU: 专门处理麻烦的浮点数（小数）运算。
          在cpu 内部都有专门的硬件电路。

        单元名称	算不算运算单元	 它算什么	   特点
        ALU	   ✅ 核心成员	  整数运算  	  最通用，延迟最低（通常1周期）
        FPU	   ✅ 专业成员	  浮点数运算	  电路复杂，延迟高（几到几十周期）
        AGU	   ✅ 特殊成员	  地址计算	    它虽然不计算"数据"，但计算"地址"，本质上也是一种加法运算。


      (4)访存（Memory Access, MEM）
        CPU 读取或写入数据内
        目的：保证数据正确存取
        硬件：D-Cache、DTLB、Load/Store Unit
        例子：LOAD R1, [R2] → 从 D-Cache/内存取值
      补充：当EX 执行的是地址计算，此步骤才会执行，否则会根据ID阶段的控制信号，进行跳过。


      (5)写回（Write Back, WB）
          CPU 把结果写回寄存器
          目的：让后续指令可以使用结果
          硬件：寄存器写端口（Register File Write Port）
          例子：ADD R1, R2, R3 → 写回 R1


  1.1.2 指令流水线的基础
    1）根本前提：指令的分解性
      一条指令的执行不是一个不可分割的原子操作，而是由一系列有序的微操作组成的。经典的指令执行周期通常包括：
      .取指 （IF）：从内存中取出指令
      .译码 （ID）：解析指令，读取寄存器，生成控制信号。
      .执行 （EX）：进行算术逻辑运算或计算地址。
      .访存 （MEM）：访问数据存储器（如果是加载或存储指令），在计算地址时用到。
      .写回 （WB）：将运算结果写回寄存器
    2) 物理基础：功能部件的独立性与并行性
      物理上的支持,流水线的每个阶段都需要由独立的硬件电路来完成。
      独立的硬件单元：CPU内部设计有多个功能独立的单元。
  
      取指单元：专门负责访问指令缓存。
      指令译码器：复杂的组合逻辑电路
      算术逻辑单元（ALU）：负责执行运算。
      数据缓存接口：负责访存。
      寄存器文件：拥有多个读写端口，允许在一个周期内同时读取和写入多个数据。

      并行工作：这些单元可以同时工作。当执行单元在处理一条指令时，取指单元可以同时去取下一条指令。这种资源在时间上的复用和空间上的并行，是流水线能够重叠执行指令的物理保障。
    
    3）时序基础：时钟周期的统一驱动
      时钟周期：CPU的主时钟提供了一个同步的节拍。流水线的每一级必须在一个时钟周期内完成其预定的工作。
      流水线寄存器：在每两个流水级之间，都有一组寄存器（称为流水线寄存器，如IF/ID寄存器、ID/EX寄存器等）。他们的作用是：
        锁存结果：在当前时钟周期结束，所存上一级产生的中间结果（指令、控制信号、计算数据等）
        隔离等级：下一级的逻辑电路直接影响到上一级
        同步传递：在下一个时钟周期开始时，将这些结果提供给下一级使用。

    4）设计原则：指令的规整性与相似性
      指令长度固定：像RISC-V或ARM的AArch64这样固定长度的指令，让取指和译码阶段变得简单。
      规则的编码格式：源操作数寄存器和目标操作数寄存器在指令中的位置相对固定，使得译码阶段可以更快地提取信息，甚至在完成译码前就可以开始准备读取寄存器。
    5）控制基础：冒险检测与处理机制
      结构冒险：当多个流水级需要访问同一个硬件资源时发生，基础是资源冗余或者流水线停顿
      数据冒险：当一条指令依赖前一条指令的计算结果时发生，基础时定向/旁路技术，即把计算结果直接从一个流水级前端短路传递给零个需要她的流水级，而不必等待他写回寄存器。
      控制冒险：当遇到跳转/分支指令时，不知道接下来取哪条指令，基础时分支预测和延迟槽等机制。

  1.1.3 流水线寄存器的由来
      在阶段之间增加 流水线寄存器（Pipeline Register）；保存阶段输出数据、控制信号，供下一个阶段使用；寄存器列表：IF/ID、ID/EX、EX/MEM、MEM/WB
     作用：
        隔离阶段
        同步流水线
        保证数据一致性：
          当当前EX的输出，是下一个EX 的输入，旁路逻辑电路会实时检测，如果是，多路选择器会将ex的输出或者ex/mem的输出端直接传递给下一个EX 阶段的输入，而不需要写回寄存器。
        通过这种方式，可以避免使用EX/MEM中的旧数据，保证数据的一致性。
      
    如果没有流水线寄存器：
        组合逻辑的“毛刺”直接传递，导致计算错误：在数字电路中，组合逻辑（如加法器、译码器）的输出并不是瞬间稳定的。由于门延迟和线路延迟，当输入变化时，
                                              输出需要一段时间才能稳定到最终值。在这段时间内，输出可能会产生多个短暂的、不正确的中间值（称为毛刺）。
        无法实现指令重叠执行，并行性丧失：无寄存器时：IF级的输出必须立即被ID级消费，也就是说，当IF级在取指令B时，它输出的值会直接覆盖掉正在被ID级使用的指令A的信息。
      
  1.1.4 流水线冲突与解决
      1）数据冲突---等数据
        场景：一条指令依赖前一条指令的计算结果。
            指令间有依赖关系，后一条指令需要前一条指令的结果才能执行。
        例子：
          I1: R1 = R2 + R3
          I2: R4 = R1 + R5
          I2 需要 I1 的结果，如果不处理，流水线会得到错误结果
      在第2条指令进入执行（EX）阶段时，第1条指令可能刚算完结果，还没写回寄存器（WB）。
       CPU就不得不插入一个“气泡”（流水线停顿），等待第1条指令的结果写回，这个周期ALU就闲置了。

      解决：
        停顿（Pipeline Stall）
            延迟 I2 的执行，直到 I1 写回
        转发（Data Forwarding / Bypass）
            将 EX 阶段的 ALU 结果直接传给 I2，避免停顿。否则需要等待R1 被写回，第二条指令ID 在重新准备寄存器。
      2）结构冲突---等资源
        场景：两个不同的流水级需要同时使用同一个硬件资源。
        例子：如果指令Cache和数据Cache是同一个（冯·诺依曼架构），当一条指令在IF阶段取指，另一条指令在MEM阶段访存时，就会冲突，必须有一个停下来等待。

      3）控制冲突---等决策
        场景：遇到分支指令（如 beq），不知道下一条该取哪条指令。
        例子：
          beq x1, x2, target  // 如果相等，跳转到target
          add x3, x4, x5      // 如果不跳转，则执行这条
          在分支指令的结果出来之前（通常在EX阶段末尾或MEM阶段），CPU只能猜测（分支预测）并提前取指。
          如果猜错了，已经进入流水线的后续指令（如 add）就白干了，必须全部清空。这造成了多个周期的完全闲置。
        解决：
          分支预测（Branch Prediction）：提前猜测。
          流水线清空（Pipeline Flush）：猜错时丢掉错误指令。
      
        传统流水线的本质问题是： 它虽然能并行，但它是静态的、按部就班的。一旦遇到依赖，就只能被动地等待，造成闲置。

  1.1.5 流水线停顿
      CPU 阶段等待数据或资源时，流水线无法前进。
      常见原因：
      1）缓存未命中（Cache Miss），MEM 阶段等待数据 → 流水线停顿。
      2）分支预测失败（Branch Misprediction），分支预测是现代处理器为了提高指令流水线效率而采用的关键技术。
        当遇到条件分支指令时，处理器会在结果出来之前猜测是否跳转以及跳转目标，并提前执行后续指令。如果猜测错误，
        已经进入流水线的推测执行指令都是无效的，必须全部清空，然后从正确的目标地址重新取指。这个过程称为分支预测失败惩罚。
            清空流水线（Pipeline Flush），失去若干个周期
      3）TLB未命中，（需要访问内存时）
        需要页表遍历（Page Walk），延迟 100+ cycles
 
1.1.6 现代 CPU 扩展
      1）为什么扩展？
          五级流水线无法充分利用指令级并行（ILP）,CPU 闲置资源多。原因是上面的三种冲突，当遇见这三种冲突是，cpu 流水线停顿
        

      2) 超标量（Superscalar） → 
        超标量流水线：每个时钟周期可以开始（取指）和执行多条指令
        物理结构：一个CPU核心内集成多条流水线
        具体过程：
        （1）取指阶段：多个取指单元，每个cycle ，可以取多条指令，并拆分
        （2）译码阶段：多个并行译码器。在同一周期内完成译码。每个译码器负责解析一条指令，生成该指令需要的控制信号。
        （3）执行阶段：多个执行单元
        （4） 寄存器：更多的读写端口，流水线寄存器还是一个，但是维度加宽了。

              超标量流水线寄存器宽度 = 单条指令信息量 × 超标量宽度
              做了什么：为了支持多个执行单元同时读取和写入数据，寄存器文件必须拥有更多的读写端口。一个简单的单端口寄存器文件一次只能读一个数，
                      而超标量CPU的寄存器文件需要同时支持多个读操作和多个写操作。比如，为了支持4条指令并行执行，可能需要8个读端口和4个写端口。
              并行体现在：多个执行单元可以同时从寄存器文件获取它们的操作数，也可以同时将结果写回，不会互相等待。

        （5）指令窗口/重排序缓冲区：更大的容量
          虽然这不完全是“复制”，但为了能让这么多指令并行且乱序执行，
            用于追踪指令状态的缓冲区（如重排序缓冲区ROB、保留站Reservation Station）的容量也必须相应增大，以便容纳更多正在执行的指令。
          
        
      3) 乱序执行（Out-of-order Execution） → 先执行可用指令
        
      4) 新增硬件：
            寄存器重命名（Register Renaming
            重排序缓冲（Reorder Buffer, ROB）
            保留站（Reservation Station）
            载入存储队列（Load Store Queue, LSQ）

  1.1.7 性能指标
        1) CPI（Cycles per Instruction） ,每条指令所需cpu周期
          CPI = 总周期 / 指令数
          理想 CPI = 1
          实际 CPI > 1，由缓存未命中、分支失败、流水线停顿等引起。
        2）IPC（Instructions per Cycle）；每cpu 周期所执行的指令数量
          IPC = 指令数 / 周期数
          超标量 CPU 理论 IPC > 1
          实际 IPC < 理论值

  1.1.8 流水线对高性能网络的影响
      1）Cache Miss → Pipeline Stall ,影响数据包处理速度
      2）Branch Misprediction → Pipeline Flush，条件分支多的 packet parser 受影响
      3）Memory Latency → 延迟执行，DMA 或大包访问影响 MEM 阶段
    prefetch 减少 MEM 阶段停顿
    if 分支可能导致控制冲突，需要 branch prediction
  
  1.1.9 相关问题
    1）CPU 是如何同时执行 200 条指令的（ROB / OOO）？
    2）一次 cache miss CPU 内部发生了什么？
    3）一次 branch mispredict CPU 内部发生了什么（流水线清空全过程）

  1.2 分支预测

  1.3 超标量执行

  1.4 指令级并行（ILP）

  1.5 SIMD / AVX
      

2 Cache体系
  2.1 L1 / L2 / L3 cache
    1）为什么需要三层cache 
      CPU执行速度远快于主存（DRAM）。如果每次访问数据都直接访问内存，CPU会大量等待。因此CPU采用 缓存层次结构（cache hierarchy),让大多数数据访问发生在更快的缓存中.
  减少cpu的等待事件。

  2）cache的结构
    现代CPU通常有三级缓存。
    层级                 大小         速度                     位置                   用途
    L1 Cache（一级缓存） 32kb         最快（4cycles）        每个核心私有           当前执行的代码和数据
    L2 Cache（二级缓存） 256KB–1MB    约12cycles             每个核心私有             L1未命中时访问
    L3 Cache（三级缓存)  8MB–64MB     约 40 cycles          多核心共享               
  3) 当cpu 需要访问变量时：load x
    会按照顺序查找，
    CPU
     ↓
    L1 hit?  yes → done
     ↓
    L2 hit?  yes → done
     ↓
    L3 hit?  yes → done
     ↓
    memory access
  4）cache hit / cache miss
    cache hit 数据已经在cache 
    cache miss 数据不在cache ,需要去内存中取

  5) cache 对性能的影响
    访问cache快，L1cache 4cycles ,L3 cache 40cycles ,而访问memory ≈ 200 cycles， 
    如果数据都在cache 中，取数据的时间就会变短。CPU等待内存的时间减少，性能就会高

    对应cache miss 就会增加cpu 周期
    L1 miss → L ≈ 10 cycles
    L2 miss → L3 ≈ 30 cycles
    L3 miss → memory  ≈ 200 cycles


CPU core
 ├─ L1 instruction cache
 └─ L1 data cache

  2.2 cache line
    Cache 并不是按字节加载的，而是以 64 字节 (Cache Line) 为基本单位。
      面试官关点： 如果你修改一个 long 类型变量，CPU 会把周围的 64 字节全部加载进缓存。也就是 spatial locality （空间局部性）
      后果： 编写代码时，数据结构如果能按照 64 字节对齐，能极大减少内存访问次数

  2.3 cache miss
    Cache miss 指 CPU 访问某个数据时，在当前缓存层级中没有找到该数据，需要到更低层级缓存或主内存读取。由于缓存层级访问延迟差异很大，cache miss 会显著影响程序性能。
      典型 CPU 的缓存结构:
        L1 Cache   （~32KB）  延迟 ≈ 3–4 cycles
        L2 Cache   （~256KB） 延迟 ≈ 10–15 cycles
        L3 Cache   （几 MB）  延迟 ≈ 30–50 cycles
        Memory     （GB）     延迟 ≈ 100–300 cycles
      访问流程：
        CPU load → L1
            ↓ miss
            L2
            ↓ miss
            L3
            ↓ miss
            Memory
      如果L1 找不到数据，就发生L1 miss ; 如果L3也找不到，就需要访问主内存，代价最高。
  2.3.1 Cache Hit 与 Cache Miss
    1）cache hit 
        假设代码，x = arr[i];
      CPU执行时，查询L1 cache ,若找到 -> L1 cache hit ;
      若没有 ->L1 cache miss, 继续在L2 cache  L3 cache 中查找，若L3 也没有，则cache miss， 则在内存中查找。读取数据后，会把数据加载到 cache 中。
    2）Cache miss 的三种经典类型
      （1）cold miss ,第一次访问某个数据时，缓存中一定没有; 特点：无法避免
      （2）Capacity miss，缓存容量不够导致的数据被替换；
         eg：L1 cache = 32kb， 数组 = 1MB, 旧数据不断被淘汰，再次访问旧数据时，已经不在cache ,因此miss.
      （3）Conflict miss ，冲突未命中
          多个数据映射到同一个 cache set。即使缓存总体还有空闲空间，但由于缓存映射策略（通常是组相联映射），多个数据块被映射到了同一个缓存组（或行），导致它们互相争抢同一个位置。
          在程序设计中，有时可以通过在数据结构中填充无用字节（Padding）或采用不同的数据布局（如SoA）来错开地址，避免冲突。
      （4）一致性未命中 (Coherence Miss)
        当多个CPU核心共享数据时，为了保持缓存一致性，如果一个核心修改了数据，另一个核心的缓存副本会失效。当另一个核心再次读取时，即使它之前缓存过，也会发生未命中。
        场景： 多线程编程中，多个核心频繁读写同一个变量（特别是伪共享（False Sharing）现象）。

        注：缓存地址的使用方法
            cpu 从内存中取出数据后，不是按顺序放入缓存的，而是采用映射的方法，将cache line存入到缓存中，
            简单来说：数据不是顺序放的，而是根据它的内存地址，通过一个公式计算出该放在哪个位置。这个算法叫做缓存映射。主要有三种方式。
            现代CPU（如你之前关注的L1缓存）几乎都采用第三种——组相联映射。
            组相联映射：缓存被分成若干个"组"（Set），每个组里有若干个"路"（Way，即缓存行）。
              内存地址先通过取模运算，决定它属于哪个组
              到了这个组里，它可以放在该组内的任意一路（任意一个缓存行位置）。
            举例（4路组相联）： L1缓存有128个组，每组可以放4个不同的内存块。

  2.4  cache locality （缓存局部性）
    指的是 CPU 频繁访问相同或附近的内存地址的趋势。它是利用 CPU 缓存机制来提升程序性能的基础。为了让程序跑得更快，我们希望数据尽可能待在 CPU 的高速缓存（L1/L2/L3 Cache）里，
而不是慢速的主内存（RAM）里。
    2.4.1 为什么需要缓存、局部性？
      速度差距：cpu 运算速度块，但访问内存（ram）速度慢（几十上百倍）。cpu 缓存（cache)是介于cpu 和 内存之间的一块小容量但极高速的存储器。
     // cache 典型访问速度， L1  1ns ； L2  3ns ;L3 12ns , ram 内存 50~100ns
      预取机制：当cpu 从内存加载数据时，不是只加载一个，而是加载一整块数据（成为cache line, 缓存行，通常为64字节）到缓存中。
      核心思想：如果程序具有良好的局部性，那么在用完当前数据后，下一个需要的数据可能已经在缓存里了（缓存命中，cache hit),如果局部性差，cpu 就得频繁的取内存取数据（缓存未命中
    cache  miss）,导致cpu 等待，程序变慢。
    2.4.2 局部性的类型
      时间局部性：如果一个内存位置被访问了一次，那么它在不久的将来很可能再次被访问。
        例子：在一个循环中反复使用同一个变量 counter，或者频繁访问 for 循环的索引 i。
        原理：一旦该变量被加载进缓存，后续的访问都能直接从缓存中快速读取。
      空间局部性：如果一个内存位置被访问，那么它附近的位置（相邻的内存地址）在不久的将来也很可能被访问。
        例子：遍历数组。当你访问 array[0] 时，CPU 会将包含 array[0] 的整块内存（Cache Line）加载进来。由于数组在内存中是连续排列的，array[1]、array[2] 很可能就在同一块缓存行中。
  访问它们时就不需要再次读取内存了
        原理：利用内存的连续性和 CPU 的预取机制。
    2.4.3 cache locality 对编程的启示
      （1）优先使用连续的数据结构：数组比链表具有更好的局部性
      （2）按内存布局顺序遍历，数据按行存储
      （3）将频繁访问的变量放在一起：如果定义了一个结构体，把经常一起用到放在相邻的位置。
      （4）循环访问数据时，步长尽量小，避免大跨度跳跃
      （5）分块处理：对于超大数据集，可以分成小块处理，确保一块数据在被cpu处理期间，始终在缓存中。

  2.5 false sharing（性能杀手）
    场景： 核心 A 修改变量 X，核心 B 修改变量 Y。如果 X 和 Y 恰好在同一个 64 字节的 Cache Line 里，L1/L2 必须频繁同步。
    后果： 即使两个核心处理不同的业务，也会因为争抢同一个 Cache Line 导致性能雪崩。
    对齐优化： 在高性能 C 代码中，我们会使用 __attribute__((aligned(64))) 来强行拉开变量距离。

    常见面试题目：
    1）深度原理：为什么修改不同的变量会互相干扰？
   面试官： “既然核心 A 改的是变量 X，核心 B 改的是变量 Y，硬件层面上它们是怎么打架的？”
    核心逻辑：
      MESI 协议： 你需要提到 MESI（Modified, Exclusive, Shared, Invalid） 协议。
      触发流程： 当核心 A 修改 Cache Line 中的 X 时，硬件会将该 Cache Line 标记为 Modified。根据一致性协议，核心 B 中包含 Y 的同一个 Cache Line 必须被标记为 Invalid。
      性能杀手： 即使核心 B 根本不关心 X，它在读取 Y 时也会发现缓存失效，被迫去 L3 甚至内存重新加载。这被称为缓存抖动（Cache Thrashing）。

  2）场景辨析：哪些代码最容易写出伪共享？
    面试官追问： “在你做的防火墙系统中，给几个具体的伪共享场景。”

  高危场景：
    统计计数器： 比如一个全局结构体存储各网卡的丢包数：struct stats { uint64_t port0_drop; uint64_t port1_drop; };。如果核 0 处理口 0，核 1 处理口 1，
        这两个计数器极大概率在同一个 Cache Line 里。
    无锁队列头尾指针： Ring Buffer 的 head 和 tail。生产者改 tail，消费者改 head，如果它们靠得太近，两边都会变慢。
    多线程共享的高频结构体： 比如一个包含锁（Spinlock）和数据的结构体。

  3）实战观测：你如何确定程序里存在伪共享
    面试官追问： “如果你发现程序变慢了，你怎么‘抓到’是伪共享干的？”
    必杀技：perf工具，使用 perf c2c (Cache-to-Cache) 命令。这是 Linux 专门用于检测伪共享的利器。
    它能直接指出哪些内存地址存在频繁的 Remote HITM（Hit Modified，即命中了其他核心已修改的缓存行），并定位到具体的代码行。

    如果没有c2c 
      即使没有 c2c 插件，标准的 perf stat 也能通过硬件计数器提供线索。
      执行命令：
      分析特征：
          高 L1-dcache-load-misses： 伪共享会导致缓存行频繁被其他核心设置为 Invalid，从而引发大量的 L1 缓存缺失
          高 IPC (Instructions Per Cycle) 却低吞吐： 如果你发现 CPU 核心非常忙（Cycles 很高），但实际处理的业务数据包（pps）上不去，且 Cache Miss 异常高，
          这就是典型的缓存颠簸。

    perf c2c 使用方法：
      （1）perf c2c record ./program 
        运行结束后生成：perf.data
      （2）查看报告，
        perf c2c report
      （3）典型输出：
        Shared Data Cache Line Table
        Cacheline        Total HITM   Store   Load
        0x7f2a8b34000       12500     8000    4500
      （4）字段含义
        Cacheline	cache line 地址
        Load	读取次数
        Store	写入次数
        HITM	hit modified
        关键指标：HITM（Hit Modified）
        此值高，说明可能存在cache false sharing
        
  4）解决方案：除了加 Padding，还有什么优雅的办法？
  面试官追问： “在 C 语言中，你会怎么写代码来彻底消除它？”
    方案 A：对齐填充 (Manual Padding
    方案 B：语言/编译器特性 (更优雅)
        使用 C11 标准的 alignas(64)。
        使用 GCC 特有的 __attribute__((aligned(64)))。
    方案 C：空间换时间 (Per-Core Data)
        为每个 CPU 核心分配独立的变量副本。例如，每个核只改自己私有的计数器，最后读取时再累加。

  5）扩展思考：伪共享一定是坏的吗？
    面试官追问： “如果我故意把两个变量放在一起，有没有好处？”
    逆向思维： * 如果两个变量总是被同一个线程同时修改，把它们放在一个 Cache Line 反而是好事。因为一次加载就能把两个都带进缓存（空间局部性），提升效率。
    关键在于：访问模式（Access Pattern）是否匹配。

  6）为什么 false sharing 程序 cache miss 很低，但性能却很差？
    因为性能瓶颈不是 cache miss，而是 cache coherence (MESI) 导致的 cache line bouncing（缓存行跳跃）。

  7）常见的性能问题
    （1） Cache Thrashing ，数据太大，超过cache
    （2）pointer chasing，随机访问，每次访问不同cache line，导致大量cache miss
    （3）False Sharing，多线程访问同一cache line ,触发cache 一致性

关键概念
  cache friendly data structure
  prefetch
  NUMA
推荐资料
  Computer Systems: A Programmer's Perspective
  Computer Architecture: A Quantitative Approach

3 工具使用
  
3.1 perf 
3.1.1 核心命令
perf stat 用来收集关键的性能计数器数据
（1）直接使用 perf stat  + 可执行程序
（2）perf stat -e + 参数  + 可执行程序
（3）perf stat -p + pid1，pid2
（4）将统计结果保存到文件
    perf stat -o perf_stats.txt ./program
（5）perf record 与 perf report：深度采样分析热点
    定期记录程序在某个时刻执行什么代码，perf report 将这些采样点汇总、统计生成 一个按cpu时间占比排序的函数列表，并将数据采记录到 perf.data 文件中。
    记录完成后，使用perf report 查看具体内容
（6）perf top
    展示占用cpu最多的函数。执行后，会看到一个实时刷新的列表，最上面是当前采样周期内cpu 占用率最好的内核或者用户空间函数。用于快速定位哪个模块、哪个函数在消耗cpu

（7）perf总结 

    perf stat  总体统计
    perf record/report  统计热点函数   
    perf mem    内存访问行为  (暂无测试)
    perf c2c    cache line sharing（暂无测试）


3.1.2 使用技巧
  （1）使用perf list 查看可用事件 
  （2）perf stat -e指定具体的事件，eg: perf stat -e cache-misses,cache-references ./num
  （3）事件的具体含义
      核心通用事件 (Basic Metrics)，这些事件用于评估程序的整体执行效率。
        cycles: CPU 周期数。反映了 CPU 实际忙碌的时间。
        instructions: 完成的指令数。
        branches: 分支指令总数（如 if, switch, 循环）。
        branch-misses: 核心指标。分支预测失败次数。如果该值较高，说明代码逻辑（如复杂的判断条件）导致 CPU 流水线频繁冲刷。
        insn per cycle (IPC): 核心指标。IPC=cyclesinstructions​/cycles。IPC 越高代表 CPU 效率越高。通常高于 1.0 较好，低于 0.5 可能存在严重的 I/O 或缓存瓶颈。
      内存与缓存事件 (Memory Hierarchy)
        cache-references: 缓存访问总次数，（LLC cache）
        cache-misses: 关键指标。缓存未命中次数。如果这个比例很高，说明你的数据结构对缓存不友好。(LLC cache)
        L1-dcache-load-misses: 一级数据缓存读取未命中。
        LLC-loads / LLC-load-misses: 末级缓存（Last Level Cache，通常是 L3）的访问与未命中。
            分析意义： 如果 LLC 经常未命中，意味着 CPU 必须频繁访问内存（RAM），这比访问缓存慢数百倍。
        dTLB-load-misses: 数据 TLB（页表缓存）未命中。
            分析意义： 如果这个值高，说明程序访问的内存地址过于分散，可能需要考虑使用 大页 (Hugepages) 来减少页表查表开销。

        管道流水线事件 (Pipeline Stalls)，这些事件可以告诉你 CPU 在执行指令时，为什么会“发呆”（Stall）。
            stalled-cycles-frontend: 指令前端停顿。
                分析意义： 说明 CPU 取指令或解码指令太慢，通常是因为代码体积太大、分支预测失败或取指缓存（ICache）未命中。
            stalled-cycles-backend: 指令后端停顿。
                分析意义： 说明 CPU 在等待数据输入或执行单元忙。这是最常见的瓶颈，通常是因为等待内存数据返回。
        内核与调度事件 (Context & System)， 如果你在做内核模块或系统级开发，关注这些软件事件。
          context-switches: 上下文切换。
              分析意义： 频繁切换会破坏缓存热度，增加内核开销。对于高性能应用，通常追求“零切换”。
          cpu-migrations: CPU 迁移。进程在不同核心间跳动，会导致本地缓存失效。
          page-faults: 缺页异常。如果大量发生，说明程序正在申请大量内存或频繁操作未映射的内存区域。

        task-clock (msec): 程序运行所占用的 CPU 总时间。
        context-switches: 上下文切换次数。如果这个值很高，说明进程频繁地被挂起或切换，可能存在激烈的锁竞争。
        cpu-migrations: 进程在不同 CPU 核心之间的迁移次数。
        page-faults: 缺页异常。
        cycles: 消耗的 CPU 周期数。
        instructions: 执行的指令数。
        branches: 分支指令数。
        branch-misses: 分支预测失败次数。如果这个比例超过 3%，通常意味着代码逻辑过于复杂或不可预测，严重影响性能。

3.1.3 在高性能系统里的判断方法
  L1 miss < 3%    L1-dcache-loads/L1-dcache-loads-misses
  LLC miss < 1%   (last level cache)   
  IPC > 1.8 (instructions per cycle)
3.1.4 评价 cache locality 的正确方法
  L1-dcache-load-misses / L1-dcache-loads，因为L1 是最频繁访问层
    <1%   非常好
    1–5%  正常
    >10%  locality 差

3.1.4  cache locality 实验 ，见《cpu性能分析基准实验》

二、Linux 内核原理
高性能开发必须理解 Linux 内核如何管理资源。
重点模块：
1 进程 / 调度
  CFS 调度器
  task_struct
  context switch
  CPU affinity
2 内存管理
  虚拟内存
  页表
  TLB
  hugepage
3 网络子系统
  socket
  sk_buff
  netfilter
  NAPI
4 IO
  epoll
  io_uring
  page cache
  block layer
推荐书：
  Understanding the Linux Kernel
  Linux Kernel Development

三、Linux 性能分析工具

数据面工程师核心能力：找到瓶颈。
必须掌握
  工具：
    perf
    ftrace
    bcc
    bpftrace
    systemtap
    flamegraph
  重要指标：
    CPU cycles
    cache miss
    context switch
    branch miss
    LLC miss
推荐资料：
    Systems Performance: Enterprise and the Cloud


四、高性能网络（核心）
数据面开发核心领域。
必须掌握：
1 TCP/IP 协议栈
  重点：
    TCP状态机
    拥塞控制
    流量控制
    RTT
    RTO
    Nagle

经典书
    TCP/IP Illustrated Volume 1

2 Linux 网络栈
  必须理解：
    网卡 -> driver -> NAPI -> softirq -> protocol stack -> socket
  重要模块：
    net_device
    sk_buff
    softirq
    GRO
    TSO
    RPS
    RFS
    XPS
3 用户态网络（高性能关键）
现代高性能网络几乎都绕过内核。
需要掌握：
  DPDK
  核心：
    PMD
    hugepage
    mbuf
    ring
    lcore
    poll mode
    AF_XDP
  结合：
    XDP
    zero copy
    umem
    XDP
    用于高速包过滤。

五、eBPF 技术
  未来网络可观测 + 数据面扩展技术。
  核心：
    BPF map
    BPF program
    kprobe
    tracepoint
    tc-bpf
    xdp-bpf
  重要工具：
    bcc
    bpftrace
    libbpf


六、高性能并发编程
核心：锁与无锁结构

须掌握：
  1 并发模型
    Reactor
    Proactor
    Actor
    SPDK reactor
  2 同步机制
    mutex
    spinlock
    rwlock
    RCU
    seqlock
  3 lock-free
    CAS
    memory ordering
    atomic
    ring buffer

七、高性能数据结构

数据面系统对数据结构要求极高。

必须掌握：

ring buffer
lock-free queue
hash table
radix tree
bloom filter
lru cache
skip list

DPDK常用：

rte_ring
rte_hash
rte_mempool



八、存储系统（如果转存储）

需要掌握：

1 IO路径
application
page cache
block layer
nvme driver
ssd
2 文件系统
ext4
xfs
btrfs
3 存储引擎
LSM tree
B+ tree
WAL
compaction

经典书：

Designing Data-Intensive Applications


九、网络与存储框架

工业界核心技术：

网络：

DPDK
FD.io VPP
Seastar
Envoy

存储：

SPDK
Ceph
RocksDB
TiKV
十、性能工程（最重要）

真正的数据面工程师能力：

benchmark
profile
optimize

常见指标：

QPS
throughput
latency
p99 latency
tail latency

测试工具：

wrk
iperf
pktgen
fio
十一、硬件层（很多人忽略）

必须理解：

NUMA
PCIe
NIC queue
RSS
SR-IOV

网卡原理：

RX ring
TX ring
descriptor
DMA


十二、数据面架构设计

优秀数据面工程师必须具备：

pipeline 设计

典型结构：

RX
↓
parser
↓
lookup
↓
action
↓
TX

例如：

VPP pipeline
DPDK pipeline
switch pipeline
十三、可观测性

现代系统必须具备：

metrics
logging
tracing

技术：

Prometheus
OpenTelemetry
eBPF observability
数据面工程师能力模型

完整能力：

硬件
↓
操作系统
↓
网络协议
↓
内核网络栈
↓
用户态网络
↓
并发编程
↓
高性能数据结构
↓
性能分析
↓
系统架构

目前缺的是：
CPU架构
Linux内核
DPDK
eBPF
性能分析

优先顺序
1 CPU + cache + NUMA
2 Linux网络栈
3 perf性能分析
4 DPDK
5 eBPF
6 lock-free数据结构


做性能实验。

例如：
cache miss 实验
false sharing 实验
NUMA 实验
DPDK benchmark

