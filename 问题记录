1. 网卡硬中断发起的时机是？
答案：不是“来一个包就发一个中断”，而是“满足特定阈值”时发起
    基础时机： 当网卡完成 DMA 传输，将数据包从网卡硬件缓冲区成功写入到内存的 Ring Buffer 后，它会向 CPU 发起中断。
    现代优化（中断风暴）： 如果 10Gbps 网卡每秒收 1488 万个小包，CPU 每秒处理 1488 万次中断就会瘫痪。所以现代网卡使用 Interrupt Coalescing（中断聚合）：
        时间阈值： 收到包后延迟一小段时间（如 10μs），等后面的包一起发。
        数量阈值： 攒够一定数量的包（如 16 个）再发一次中断。


2. CPU 是如何检测中断发生的？（最硬核的问题）
CPU 并不是在运行代码时突然“感知”到了中断，而是靠硬件电平信号和指令周期的空隙。
第一步：物理信号（电线）：
网卡通过 PCIe 总线的特定信号线（或发送一个特殊的 MSI-X 消息包）通知 中断控制器（APIC）。APIC 随后会将 CPU 的一个物理引脚（INTR）拉高电平。
第二步：指令间隙检查：
CPU 在执行每一条汇编指令的最后一个时钟周期，都会自动去检查那个物理引脚的电平状态。
    如果引脚是高电平，且当前 CPU 的中断屏蔽位（IF位）允许中断。
    CPU 会强行停止下一条指令的取指操作。
第三步：硬件自动保存现场：
CPU 硬件（不是软件！）会自动将当前的 程序计数器（RIP）、标志位（EFLAGS） 等压入内核栈。
第四步：跳转：
CPU 根据中断号（Vector），去内存里的 IDT（中断描述符表） 查找对应的处理函数地址，然后强行修改 RIP 跳过去执行。


3，CPU 硬件（不是软件！）会自动将当前的 程序计数器（RIP）、标志位（EFLAGS） 等压入内核栈。 这里的硬件和软件怎么理解？
（1） 软件（Software）的理解：必须有指令控制
    软件行为是指 CPU 必须执行了某条具体的二进制指令（如 PUSH, MOV, ADD），才会发生状态改变。
    特征： 程序员可见。你可以通过反汇编看到这一步。
    例子： 当你写 a = b + 1，编译器生成 ADD 指令。CPU 读到这条指令，电路才去算加法。这就是软件行为。
    在中断中： 进入中断处理函数后，你写的 push rbp（保存基址指针）就是软件行为
（2）硬件（Hardware）的理解：固化在硅片里的逻辑，（不需要应用或者内核程序员开发的命令，是cpu提供的机制）
    硬件行为（又叫微操或原子行为）是指 CPU 在不执行任何软件指令的情况下，根据内部电路逻辑自动完成的一系列动作。
    特征： 不可拆分、不可跳过、对程序员透明。你找不到任何一条指令叫“把 RIP 压栈并跳转”，但它就是发生了。
（3）中断发生时，硬件和软件都发生了什么？
    第一阶段：硬件自动动作（硬接力）
        当 CPU 的中断引脚检测到高电平，且当前指令执行完的瞬间，CPU 内部的**状态机（State Machine）**启动，自动完成以下动作：
        切栈： CPU 自动从用户态栈顶（SS/RSP）切换到内核栈顶。
        压栈： 硬件逻辑像“强行塞入”一样，把 SS、RSP、RFLAGS、CS、RIP 依次推入内核栈。
        加载入口： 硬件去内存的 IDT 表里读取地址，直接覆盖当前的 RIP。
        为什么要硬件做？ 因为此时 RIP 还没跳到内核，软件根本没机会执行任何指令。如果硬件不自动保存 RIP，CPU 就像失忆了一样，处理完中断后找不到回家的路。
    第二阶段：软件手动动作（软接力）
        此时 RIP 已经指向了内核驱动的中断处理函数。
        保存剩余现场： 硬件只帮你了保存了最核心的几个寄存器，剩下的 RAX, RBX, RCX 等通用寄存器，需要你在代码里写 SAVE_ALL 宏或手动 push。
        处理业务： 执行你的防火墙逻辑或驱动逻辑。


4，中断为什么耗时？
    硬件压栈的耗时： 硬件自动压那 5 个寄存器，涉及到多次内存写操作（访问内核栈）。这虽然是硬件做的，但它依然要消耗 CPU 周期（大约几十个时钟周期）。
    流水线清空（Pipeline Flush）： 硬件在执行这个自动跳转时，必须把当前流水线里所有“预取”的用户态指令全部扔掉。
    TLB 抖动： 硬件切换特权级时，可能会导致 TLB 缓存的访问权限失效。

5，应对面试的思维导图：
    硬件行为： 自动压栈（RIP/EFLAGS）、切栈、查 IDT 表、切 CPL（从 3 到 0）。
    软件行为： 保存通用寄存器、执行驱动逻辑、清空中断标志。

6，Socket 缓冲区溢出是怎么回事？”
    “我理解 Socket 缓冲区本质上是内核 struct sock 中的两个 sk_buff 双向链表。
    它们在执行 socket() 系统调用时被初始化，并根据系统内核参数设定初始配额。
    当协议栈处理完数据包后，会尝试将其挂载到接收队列。如果应用程序处理太慢（recv 不及时），导致链表占用的内存达到了 sk_rcvbuf 的上限，内核就会直接丢弃后续到达的包。
    我们可以通过调大 tcp_rmem 或者在代码中通过 setsockopt(SO_RCVBUF) 来手动调整这个容器的大小。”



